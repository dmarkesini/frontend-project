const TopicTitle = ({ title }) => {
  return (
    <div className="articles_header-container">
      <h1 className="articles_title">{title} articles</h1>
    </div>
  );
};

export default TopicTitle;
